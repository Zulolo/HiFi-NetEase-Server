# tools/ · Development and measurement helpers

- `bench/` scripts to measure on the real board what docs/05 estimates:
  DSD-to-PCM CPU cost, resampler CPU cost, USB disk throughput, upload throughput,
  RAM footprint of mpd + hifid.

Results are written back into docs/05 (section "Measured on hardware").
